#include "carta.h"
#include <vector>
class Jugador{
    public:
        string nombre;
        vector<Carta> mano;
        int acopmulador;
        Jugador(string nom : string nombre(nom)){}

        Carta tirar_carta(int i){
            return mano[i]
        };
        
}